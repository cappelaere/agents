# Langfuse

```
git clone https://github.com/langfuse/langfuse.git
cd langfuse
```
Might want to edit the docker-compose.yml file and the CHANGE ME
Make sure to adjust the security group

```
docker compose up

export LANGFUSE_HOST=150.240.3.116
```
bring up http://$HOST:3000
sign up
create organization and project
generate API Key

